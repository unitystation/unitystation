
using UnityEngine;

namespace Objects.Wallmounts
{
	/// <summary>
	/// Identifies an object that, rather than being mounted directly over a wall, hangs off of it, like
	/// a lightbulb or camera.
	/// </summary>
	public class WallProtrusion : MonoBehaviour
	{
		//TODO: Currently only a marker, but will have construction specific logic eventually.
	}
}
